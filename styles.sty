\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{styles}[styles for diploma]

\usepackage{substitutefont} % so you can change babel's fonts


% Use cyrilic
\usepackage[T2A]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[russian]{babel}


% Times New Roman font package
%\usepackage{mathptmx}
% newtxtext package has to be loaded after babel
% as it meant in documentation
\usepackage{newtxmath}
\usepackage{amsmath}

% argmin and argmax
\DeclareMathOperator*{\argmin}{argmax}
\DeclareMathOperator*{\argmax}{argmax}

% Set line space to 1.5
\renewcommand{\baselinestretch}{1.5}

% Set document margins
\usepackage[left=3cm, right=1.5cm, top=2cm, bottom=2cm]{geometry}

% Set paragraph indentation
\setlength{\parindent}{1.25cm}

% Package which provides text justification
\usepackage{ragged2e}

% Use subfiles
\usepackage{subfiles}

% Define new behaviour of chapter and section command
\usepackage{titlesec}
\usepackage{etoolbox}
% ...chapter
\titleformat{\chapter}{\normalsize\bfseries}{\thechapter.}{1em}{}
\titlespacing{\chapter}{\parindent}{0pt}{0pt}
% ...section
\titleformat{\section}{\normalsize\bfseries}{\thesection.}{1em}{}
\titlespacing{\section}{\parindent}{0pt}{0pt}
% ...section
\titleformat{\subsection}{\normalsize\bfseries}{\thesubsection.}{1em}{}
\titlespacing{\subsection}{\parindent}{0pt}{0pt}

% Make Bibliography appear in table of contents
\usepackage[nottoc,numbib]{tocbibind}

% Theorems
\newtheorem{definition}{Определение}[section]

% Images
\usepackage{graphicx}
\usepackage{float}
\graphicspath{ {../plots/} }

% Possible solutions for condition formatting
\usepackage{array,tabularx}
\newenvironment{conditions}
{\par\vspace{\abovedisplayskip}\noindent
	\begin{tabular}{>{$}l<{$} @{} >{${}}c<{{}$} @{} l}}
	{\end{tabular}\par\vspace{\belowdisplayskip}}

\newenvironment{conditions*}
{\par\vspace{\abovedisplayskip}\noindent
	\tabularx{\columnwidth}{>{$}l<{$} @{}>{${}}c<{{}$}@{} >{\raggedright\arraybackslash}X}}
{\endtabularx\par\vspace{\belowdisplayskip}}

% For cross referencing
\usepackage{hyperref}

% Make Pdf-file generated by PdfLatex search-able and copy-able for pdf viewers
\usepackage{cmap}

% Lists and etc.
\usepackage{enumitem}
\makeatletter
\AddEnumerateCounter{\asbuk}{\russian@alph}{щ}
\makeatother
% Отступы по МУ-12-2004
\setlist[enumerate]{nosep, leftmargin=*, labelindent=\parindent, itemindent=0cm}
\setlist[itemize]{nosep}
% Нумерация первого уровня -- буквы со скобкой,
% второго уровня -- цифры со скобкой.
% Согласно ГОСТ 7.32-2001
\setlist[enumerate,1]{label=\asbuk*), ref=\asbuk*)}
\setlist[enumerate,2]{label=\arabic*), ref=\arabic*}
% Маркеры первого уровня.
% \setlist[itemize,1]{label=-} % ГОСТ 7.32-2001
\setlist[itemize,1]{label=\textbf{--}} % МУ-12-2004
\setlist[itemize,2]{label=\textbullet} % МУ-12-2004

% table captions
\usepackage{caption}

% Подписи
\addto\captionsrussian{\renewcommand{\contentsname}{Содержание}}
\addto\captionsrussian{\renewcommand{\listfigurename}{Список иллюстраций}}
\addto\captionsrussian{\renewcommand{\listtablename}{Список таблиц}}
\addto\captionsrussian{\renewcommand{\bibname}{Литература}}
\addto\captionsrussian{\renewcommand{\indexname}{Index}}
\addto\captionsrussian{\renewcommand{\figurename}{Рисунок{}}}
\addto\captionsrussian{\renewcommand{\tablename}{Таблица}}
\addto\captionsrussian{\renewcommand{\partname}{Часть}}
\addto\captionsrussian{\renewcommand{\chaptername}{Глава}}
\addto\captionsrussian{\renewcommand{\appendixname}{Приложение}}

% Переносы сложных слов